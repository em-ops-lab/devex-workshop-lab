# SSH Public Key for git authentication with host SSH agent forwarding
# Replace with your actual SSH public key (e.g., from ~/.ssh/id_ed25519.pub on your host)
SSH_PUBLIC_KEY="XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
